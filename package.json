{
  "name": "hopverk1",
  "version": "1.0.0",
  "description": "hopverkefni1",
  "main": "index.js",
  "scripts": {
    "lint-css": "stylelint styles.css --syntax css",
    "lint-scss": "stylelint styles.scss --syntax scss",
    "dev": "npm-run-all --parallel sass browser-sync",
    "browser-sync": "browser-sync start --server --files index.html styles.css pages/*.html",
    "sass": "node-sass --watch -r styles.scss styles.css"
  },
  "author": "EMJ",
  "license": "ISC",
  "dependencies": {
    "stylelint-scss": "^3.12.0"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "node-sass": "^4.12.0",
    "npm-run-all": "^4.1.5",
    "stylelint": "^11.1.1"
  }
}
